{
  "name": "v2pi-e2e",
  "private": true,
  "version": "0.0.0",
  "description": "Full-stack Playwright suite for the v2pi panel at phone, laptop and desktop widths.",
  "scripts": {
    "build:spa": "npm --prefix ../frontend-next ci && npm --prefix ../frontend-next run build",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}

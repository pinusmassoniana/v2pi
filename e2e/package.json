{
  "name": "v2pi-e2e",
  "private": true,
  "version": "0.0.0",
  "description": "Full-stack Playwright smoke for the v2pi panel (first-run setup → login → dashboard).",
  "scripts": {
    "build:spa": "npm --prefix ../frontend ci && npm --prefix ../frontend run build",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
